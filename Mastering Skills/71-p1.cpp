#include <iostream>
using namespace std;

int main() 
{
    int row;
    int counter = 1;
    cin >> row;
    while(counter <= row)
    {
        cout << "*";
        counter++;
        while (counter > row)
    }
}